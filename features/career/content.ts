/**
 * ─────────────────────────────────────────────────────────
 *  ✏️ 원고는 여기 없다. content/profile.json 을 고치고 커밋하면 바뀐다.
 * ─────────────────────────────────────────────────────────
 * 이 파일은 feature 안에서 쓰기 좋은 이름으로 다시 내보내기만 한다 —
 * 원고의 출처가 바뀌어도(파일 → DB → CMS) 화면 코드는 안 건드리게 하려는 것이다.
 */

export type { CareerEntry } from "@/shared/content";
export { CAREER_ENTRIES, CAREER_INTRO } from "@/shared/content";
