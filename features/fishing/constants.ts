import type { Vec2XZ } from "@/shared/types";

export const FISHING_PANEL_ID = "fishing.spot";

/**
 * 낚시터 자리. **하트의 아래 꼭지**, 섬 남쪽 끝 물가다.
 *
 * 사진첩이 동쪽이고 방명록이 북쪽이니 낚시터는 남쪽이어야 셋이 섬을 고루 나눈다.
 *
 * ⚠ 남쪽이라고 **정남향 물가**에 두면 안 된다.
 *   카메라는 방위가 고정이라 늘 남쪽 위에 떠서 북쪽을 본다. 정남향 물가에서
 *   던지면 찌가 **카메라 쪽으로** 날아와 화면 아래로 빠져나간다. 예전에 실제로
 *   그래서 낚시터를 북쪽으로 옮겼었다.
 *
 *   하트의 아래 꼭지는 남쪽으로 길게 뻗은 모래톱이라 **옆면이 서쪽을 향한다.**
 *   그 옆면에 서면 위치는 남쪽이면서 던지는 방향은 옆이라, 찌가 화면 안에 남는다.
 *   (해안 법선 약 140° → 화면 중앙에서 가로 23°, 아래 33°. 화각은 가로 ±33°, 아래 3~43°)
 */
export const FISHING_POSITION: Vec2XZ = [-8.1, 50.5];
/** 다가가 서는 자리. 낚시터 뒤(섬 안쪽)에 서서 바다를 본다. */
export const FISHING_APPROACH: Vec2XZ = [-5.6, 48.5];

/**
 * 던지는 방향. **거의 정서향**(195°)이다.
 *
 * ⚠ 예전엔 이 방향을 접근점 → 낚시터 벡터에서 뽑았다. 기하학적으로는 그럴듯한데
 *   그 벡터가 서남서(141°)라, 찌가 화면 **왼쪽 아래 구석**에 떨어졌다. 찌가
 *   까딱이는 것만 보던 시절엔 그래도 됐지만, 챈 뒤에 물보라가 튀고 뭔가
 *   튀어나오게 만들고 나니 정작 그 장면이 프레임 모서리에서 벌어졌다.
 *
 *   카메라는 방위가 고정이고 늘 북쪽을 본다. 그러니 **서쪽으로 던지면 찌는
 *   캐릭터와 같은 높이의 화면 왼쪽**에 놓인다 — 남쪽으로 던질 때처럼 아래로
 *   빠지지 않는다. 15° 만 북쪽으로 틀어 화면 중앙 쪽으로 조금 당겨온다.
 *
 *   이 방향의 물 깊이는 6m 앞에서 -1.9m 다. 얕은 데 떨어지면 찌가 모래 위에
 *   놓인 것처럼 보인다.
 */
const CAST_ANGLE = (195 * Math.PI) / 180;
export const CAST_AIM: Vec2XZ = [Math.cos(CAST_ANGLE), Math.sin(CAST_ANGLE)];
